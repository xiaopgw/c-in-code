# 知识掌握报告
生成时间: 2026-07-06

## 各知识点掌握程度

| 知识点 | 总题数 | 正确数 | 掌握率 | 状态 |
|--------|--------|--------|--------|------|
| break vs return | 1 | 0 | 0% | 薄弱 |
| 指针别名（多指针指向同一内存） | 1 | 0 | 0% | 薄弱 |
| 链表遍历边界条件 | 1 | 0 | 0% | 薄弱 |
| **时间复杂度分析** | **1** | **0** | **0%** | **薄弱** |
| **顺序存储 vs 链式存储** | **1** | **0** | **0%** | **薄弱** |
| **双向链表 vs 单向链表** | **1** | **0** | **0%** | **薄弱** |
| **二叉树递归函数识别** | **1** | **0** | **0%** | **薄弱** |
| **栈的应用（括号匹配）** | **1** | **0** | **0%** | **薄弱** |
| **二叉树递归编程** | **1** | **0** | **0%** | **薄弱** |

## 薄弱知识点（掌握率低于80%）

### 数据结构卷一新增（2026-07-06）

- **时间复杂度分析**：嵌套循环的时间复杂度不能只看有几层嵌套，还要看每层的增长方式（i++ 是 n，i*=2 是 log n，i+=i 也是 log n）。外层 log n * 内层 n = O(n log n)，不是 O(n^2)。
  - 相关错题：卷一选择题第2题

- **顺序存储 vs 链式存储**：链式存储每个节点多一个指针域（8字节），空间利用率比顺序存储更低。不要误以为"链式存储没有容量限制所以空间利用率更高"。
  - 相关错题：卷一选择题第3题

- **双向链表 vs 单向链表**：按位置查找第 n 个节点，两者都需要遍历 O(n)，双向链表没有优势。删除已知节点和反向遍历才是双向链表的优势。
  - 相关错题：卷一选择题第22题

- **二叉树递归函数识别**：判断每个递归函数的终止条件和返回值含义。叶子节点条件是 `root->lchild==NULL && root->rchild==NULL`。所有节点个数是对每个节点都加1，叶子个数只对叶子节点加1。
  - 相关错题：卷一选择题第25题

- **栈的应用（括号匹配）**：标准做法是用栈。左括号入栈，右括号出栈对比类型。用标志位无法处理嵌套结构。
  - 相关错题：卷一编程题第3题

- **二叉树递归编程**：满二叉树判定需要理解递归求深度 + 递归求节点数 + 公式 2^k - 1 的综合运用。
  - 相关错题：卷一编程题第5题

### 此前保留（2026-06-29）

- **break vs return**：return 结束整个函数，break 只跳出当前循环。在链表的输入循环中误用 return -1 导致打印代码无法执行。
  - 相关表现：输入 -1 后程序直接退出，后面的打印循环没机会运行

- **指针别名（多指针指向同一内存）**：当 `ptail = hp` 后，ptail 和 hp 指向同一块内存，通过 ptail 修改内容，hp 看到的是同一份修改。
  - 相关表现：不理解为什么修改 ptail->poit 会影响 hp 的链表结构

- **链表遍历边界条件**：遍历时先移动指针再访问数据，可能导致 p 为 NULL 时访问 p->ele 产生段错误，或数据多打少打。
  - 相关表现：遍历顺序颠倒导致段错误，-1 标志被多打印出来

## 历史薄弱-已掌握知识点
- （暂无）

## 学习建议
1. **时间复杂度**：多做几道嵌套循环的时间复杂度练习题，重点是识别每一层的增长模式（线性/对数/平方）
2. **顺序存储 vs 链式存储**：记住一个对比表——顺序存储连续/固定/随机存取/无指针开销，链式存储不连续/动态/顺序存取/有指针开销
3. **双向链表 vs 单向链表**：背下四个关键操作的差异——删除已知节点（O(1) vs O(n)）、反向遍历（支持 vs 不支持）、按位置查找（O(n) vs O(n)）、按值查找（O(n) vs O(n)）
4. **递归函数**：记住三个经典递归函数的写法区别——求深度(max+1)、求节点数(左右和+1)、求叶子数(左右和+1只在叶子时加)
5. **括号匹配**：强烈建议把这个题重新写一遍，遇到不会的可以看答案文件中的参考代码
6. **链表遍历边界**：记住一个口诀 —— "先判断是不是 NULL，再访问；先访问当前节点，再移动指针"
