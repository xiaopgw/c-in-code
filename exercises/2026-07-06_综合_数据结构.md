# 数据结构综合测试卷

**总分**：100分  
**时间**：120分钟  
**出题范围**：顺序表、单向链表、双向链表、顺序栈、链式栈、循环队列、链式队列、二叉树  
**说明**：所有编程题请在C语言环境下作答，可使用标准库函数（malloc、free等）

---

# 第一部分：选择题（共25题，每题2分，共50分）

---

**1.** 以下哪个场景最适合用**树形结构**来描述？
A. 食堂排队打饭  
B. 浏览器的历史记录  
C. 公司组织架构  
D. 地铁线路图  

你的答案: _____

---

**2.** 以下代码片段的时间复杂度是：
```c
for (int i = 1; i <= n; i *= 2)
{
    for (int j = 0; j < n; j++)
    {
        printf("%d ", j);
    }
}
```
A. O(n)  
B. O(n log n)  
C. O(n^2)  
D. O(log n)  

你的答案: _____

---

**3.** 关于顺序存储和链式存储，以下说法**错误**的是：
A. 顺序存储的内存空间是连续的  
B. 链式存储的插入操作不需要移动元素  
C. 顺序存储支持随机存取，链式存储不支持  
D. 链式存储的空间利用率总是高于顺序存储  

你的答案: _____

---

**4.** 一个算法的执行时间与问题规模n的关系为 `T(n) = 3n^2 + 100n + 2000`，该算法的时间复杂度属于：
A. O(n)  
B. O(n^2)  
C. O(1)  
D. O(n log n)  

你的答案: _____

---

**5.** 已知顺序表的结构体定义为：
```c
#define N 64
typedef struct seplist
{
    int data[N];
    int last;
}ss, *sp;
```
当前 `p->last = -1`，以下说法正确的是：
A. 顺序表中有一个有效元素  
B. 顺序表为空  
C. 顺序表已满  
D. 有效元素个数为 1  

你的答案: _____

---

**6.** 一个顺序表当前状态为 `data = [10, 20, 30, 40, 50]`，`last = 4`。现在要在 pos=1 的位置插入数据 15，需要移动多少个元素？
A. 2 个  
B. 3 个  
C. 4 个  
D. 5 个  

你的答案: _____

---

**7.** 承接上题，插入完成后的 `last` 值是多少？
A. 4  
B. 5  
C. 6  
D. 3  

你的答案: _____

---

**8.** 顺序表在结构体中封装了 `last` 成员，它最主要的作用是：
A. 记录数组的总容量  
B. 记录数组首地址  
C. 区分数组中有效数据和无效数据  
D. 提高程序运行速度  

你的答案: _____

---

**9.** 有头单向链表遍历时，以下哪段代码是正确的（假设 head 是头节点）？
A.
```c
while (head != NULL)
{
    head = head->next;
    printf("%d ", head->data);
}
```
B.
```c
while (head->next != NULL)
{
    head = head->next;
    printf("%d ", head->data);
}
```
C.
```c
head = head->next;
while (head->next != NULL)
{
    printf("%d ", head->data);
    head = head->next;
}
```
D.
```c
while (head->next == NULL)
{
    head = head->next;
    printf("%d ", head->data);
}
```

你的答案: _____

---

**10.** 在有头单向链表的头插操作中，以下两行代码：
```c
// 假设 new_node 是已创建的新节点
new_node->next = head->next;   // 语句1
head->next = new_node;          // 语句2
```
如果交换了语句1和语句2的执行顺序，会导致什么后果？
A. 程序正常执行，没有影响  
B. 新节点插入成功，但丢失了原有链表的第一个有效节点  
C. 新节点插入成功，但丢失了头节点  
D. 程序会段错误  

你的答案: _____

---

**11.** 一个单向链表有头节点，有效节点为 `1 -> 2 -> 3 -> 4 -> NULL`。执行逆转算法后，链表状态变为：
A. `4 -> 3 -> 2 -> 1 -> NULL`  
B. `1 -> 2 -> 3 -> 4 -> NULL`（不变）  
C. `1 -> 2 -> 3 -> 4 -> head`  
D. `1 -> NULL`  

你的答案: _____

---

**12.** 在单向链表中按值删除所有匹配元素时，为什么通常需要两个指针（一个记录前驱节点，一个遍历当前节点）？
A. 为了让程序跑得更快  
B. 因为删除操作需要同时释放两个节点  
C. 因为删除节点时需要让前驱的 next 指向要删节点的后继，不知道前驱则链表会断  
D. 因为 C 语言不允许只用一个指针操作链表  

你的答案: _____

---

**13.** 入栈顺序为 `A, B, C, D`，以下哪个**不可能**是出栈顺序？
A. A, B, C, D  
B. D, C, B, A  
C. D, A, B, C  
D. B, A, D, C  

你的答案: _____

---

**14.** 顺序栈的初始状态 `top = -1`，执行连续 5 次入栈操作后，`top` 的值是：
A. 5  
B. 4  
C. 6  
D. -1  

你的答案: _____

---

**15.** 关于链式栈，以下说法正确的是：
A. 链式栈的入栈操作使用尾插法  
B. 链式栈需要判满，因为空间有限  
C. 链式栈的入栈和出栈都是 O(1)  
D. 链式栈的节点不需要指针域  

你的答案: _____

---

**16.** 以下哪个场景最适合用**队列**而不是栈来实现？
A. 递归函数的调用返回  
B. 文本编辑器的撤销操作  
C. 打印机的任务调度（先提交的先打印）  
D. 编译器中括号匹配的检查  

你的答案: _____

---

**17.** 循环队列（容量 MAX_SIZE=6）当前 `front=2, rear=5`。此时队列的状态是：
A. 空队列  
B. 满队列  
C. 队列中有 3 个元素  
D. 队列中有 4 个元素  

你的答案: _____

---

**18.** 循环队列中，元素个数（队列长度）的计算公式是（容量为 max）：
A. `(rear - front + max) % max`  
B. `rear - front`  
C. `(rear + front) % max`  
D. `rear % front`  

你的答案: _____

---

**19.** 循环队列中用牺牲一个元素空间的方法来区分空和满。已知 `front=0, rear=0` 时队列为空，那么队列满的条件是（容量为 max）：
A. `(rear + 1) % max == front`  
B. `rear == front`  
C. `rear + 1 == front`  
D. `(rear - 1) % max == front`  

你的答案: _____

---

**20.** 双向链表中，已知一个节点指针 `p`，要删除这个节点。以下哪行代码是单向链表不需要、但双向链表必须做的？
A. `p->prev->next = p->next`  
B. `if (p->next != NULL) p->next->prev = p->prev`  
C. `free(p)`  
D. `p = NULL`  

你的答案: _____

---

**21.** 双向链表尾插操作比单向链表尾插操作**多**修改了哪个指针？
A. 新节点的 `next`  
B. 原尾节点的 `next`  
C. 新节点的 `prev`  
D. 头节点的 `prev`  

你的答案: _____

---

**22.** 以下哪个操作，双向链表的效率**不一定**优于单向链表？
A. 删除已知节点（已有该节点指针）  
B. 从尾部向头部遍历  
C. 按位置查找第 n 个节点  
D. 删除最后一个节点（已知尾指针）  

你的答案: _____

---

**23.** 对以下二叉树进行中序遍历，结果是：
```
          [A]
         /   \
       [B]   [C]
      /  \
    [D]  [E]
```
A. A B D E C  
B. D B E A C  
C. D E B A C  
D. D B E C A  

你的答案: _____

---

**24.** 一棵二叉树中，叶子节点（度为0的节点）有 6 个，度为 2 的节点有多少个？
A. 4  
B. 5  
C. 6  
D. 7  

你的答案: _____

---

**25.** 阅读以下递归函数：
```c
int func(bin_node_p root)
{
    if (root == NULL) return 0;
    if (root->lchild == NULL && root->rchild == NULL)
        return 1;
    return func(root->lchild) + func(root->rchild);
}
```
该函数的功能是：
A. 计算二叉树的深度  
B. 计算二叉树的所有节点个数  
C. 计算二叉树的叶子节点个数  
D. 计算二叉树中度为 1 的节点个数  

你的答案: _____

---

# 第二部分：编程题（共5题，每题10分，共50分）

---

**1. 顺序表合并（10分）**

现有如下顺序表结构体定义：
```c
#define N 64

typedef struct seplist
{
    int data[N];
    int last;
}ss, *sp;
```

请实现函数 `int seplist_merge(sp dest, sp src)`，将顺序表 `src` 中的所有有效元素合并到 `dest` 的末尾（追加到 dest 原有有效元素之后）。

要求：
- 如果合并后超出容量（`dest->last + 1 + src->last + 1 > N`），返回 -1，不进行合并
- 如果 src 为空表，返回 0，不进行操作
- 合并成功返回 0
- 不得使用任何辅助数组

```c
int seplist_merge(sp dest, sp src)
{
    // 请在此处实现
}
```

---

**2. 单向链表删除中间节点（10分）**

单向链表节点定义：
```c
typedef struct node
{
    int data;
    struct node *next;
}node_t, *node_p;
```

链表采用**有头链表**结构（head 为头节点，不存数据）。

请实现函数 `int delete_middle(node_p head)`，删除链表的**中间节点**。
- 如果有效节点个数为奇数，删除中间那个
- 如果有效节点个数为偶数，删除中间偏后的那个
- 例如：`1->2->3->4->5` 删除 3，`1->2->3->4` 删除 3
- 如果链表为空或只有一个有效节点，返回 -1
- 删除成功返回 0

**提示**：可以使用快慢指针法找到中间节点

```c
int delete_middle(node_p head)
{
    // 请在此处实现
}
```

---

**3. 括号匹配（10分）**

请实现函数 `int is_balanced(char *expr)`，判断一个仅包含大中小括号 `{ } [ ] ( )` 的表达式是否匹配。

匹配规则：
- 左括号必须与相同类型的右括号匹配
- 左括号必须以正确的顺序闭合
- `{[()]}` 匹配返回 1
- `{[(])}` 不匹配返回 0
- `(]` 不匹配返回 0

你可以使用以下顺序栈（假设已实现）：
```c
// 顺序栈操作（假设已实现）
sp create_stack();                 // 创建空栈
int push_stack(sp s, char c);      // 入栈
int pop_stack(sp s, char *c);      // 出栈
int is_empty_stack(sp s);          // 判空
void destroy_stack(sp s);          // 销毁
```

要求：
- 必须使用栈来实现
- 遇到左括号入栈，遇到右括号出栈检查是否匹配

```c
int is_balanced(char *expr)
{
    // 请在此处实现
}
```

---

**4. 循环队列逆序（10分）**

循环队列结构体定义（已假设实现队列的基本操作）：
```c
#define MAX_SIZE 8

typedef struct loop_queue
{
    int data[MAX_SIZE];
    int front;   // 队头下标
    int rear;    // 队尾下标
}lq_t, *lq_p;
```

已知队列的基本操作函数（已实现）：
```c
lq_p create_queue();                       // 创建空队列
int enqueue(lq_p q, int x);               // 入队
int dequeue(lq_p q, int *x);              // 出队
int is_empty_queue(lq_p q);               // 判空
int is_full_queue(lq_p q);                // 判满
```

顺序栈的基本操作函数（已实现）：
```c
sp create_stack();                         // 创建空栈
int push_stack(sp s, int x);              // 入栈
int pop_stack(sp s, int *x);              // 出栈
int is_empty_stack(sp s);                 // 判空
```

请实现函数 `void reverse_queue(lq_p q)`，将循环队列 q 中的元素顺序逆序。
- 要求不能使用额外的数组，但可以使用栈作为辅助
- 思路：先将队列中所有元素出队并入栈，再将栈中所有元素出栈并入队

```c
void reverse_queue(lq_p q)
{
    // 请在此处实现
}
```

---

**5. 判断满二叉树（10分）**

二叉树节点定义：
```c
typedef int datatype;
typedef struct binary_node
{
    datatype data;
    struct binary_node *lchild;
    struct binary_node *rchild;
}bin_node_t, *bin_node_p;
```

请实现函数 `int is_full_binary_tree(bin_node_p root)`，判断一棵二叉树是否为**满二叉树**。

满二叉树的定义：
- 除了叶子节点外，每个节点都有两个子节点（即每个节点的度要么是 0，要么是 2）
- 所有叶子节点都在同一层

例如：
```
满二叉树：
          [1]
         /   \
       [2]   [3]
      /  \   /  \
    [4] [5] [6] [7]

不是满二叉树：
          [1]          （原因：节点2的度是1，不满足度要么0要么2）
         /   \
       [2]   [3]
      /  \
    [4]  [5]

不是满二叉树：
          [1]          （原因：叶子节点不在同一层，节点3的叶子更深）
         /   \
       [2]   [3]
      /  \
    [4]  [5]
```

**提示**：可以先用递归计算树的高度和节点总数，然后利用满二叉树的性质进行判断。

```c
int is_full_binary_tree(bin_node_p root)
{
    // 请在此处实现
}
```

---

# 试卷结束

请在答题区域作答。
选择题直接在选项上打勾或写字母，编程题写出完整的函数实现。