# 数据结构综合测试卷 -- 卷二

**总分**：100分  
**时间**：120分钟  
**出题范围**：数据结构基础、顺序表、单向链表、单向循环链表、双向链表、双向循环链表、顺序栈、链式栈、循环队列、链式队列、二叉树  
**说明**：所有编程题请在C语言环境下作答，可使用标准库函数（malloc、free等）

---

# 第一部分：选择题（共25题，每题2分，共50分）

---

**1.** 以下关于"数据"的描述，正确的是：
A. 数据就是数字，只包含整数和浮点数  
B. 数据元素是数据的最小单位  
C. 数据项是描述数据元素的有用信息，是数据的最小单位  
D. 数据元素又叫节点，一个数据元素只能包含一个数据项  

你的答案: __c___

---

**2.** 算法的五个特性包括：有穷性、确定性、可行性、输入和：
A. 正确性  
B. 输出  
C. 高效性  
D. 健壮性  

你的答案: __b___

---

**3.** 以下哪种存储结构在存储数据时需要建立一张附加的索引表？
A. 顺序存储  
B. 链式存储  
C. 索引存储  
D. 散列存储  

你的答案: __c___

---

**4.** 以下代码的时间复杂度是：
```c
int i = 0, s = 0;
while (s < n)
{
    i++;
    s = s + i;
}
```
A. O(n)  
B. O(√n)  
C. O(n^2)  
D. O(log n)  

你的答案: _____

---

**5.** 关于时间复杂度的说法，正确的是：
A. T(n) = O(f(n)) 中，n 表示算法中语句的条数  
B. 一个算法的时间复杂度越低，代码行数一定越少  
C. 两个算法的时间复杂度相同，不代表它们的实际运行时间一定相等  
D. O(1) 表示算法只执行 1 条语句  

你的答案: __c___

---

**6.** 顺序表采用结构体封装数组和 last 下标，其最主要的目的是：
A. 减少代码行数  
B. 将数据和描述数据的信息（有效元素个数）绑定在一起  
C. 提高程序的运行速度  
D. 使数组支持动态扩容  

你的答案: __b___

---

**7.** 在长度为 n 的顺序表中删除第 i 个元素（0 <= i < n），需要移动元素的个数是：
A. i  
B. n - i  
C. n - i - 1  
D. n  

你的答案: _____

---

**8.** 有头单向链表在遍历时，常用的循环条件是 `while (p->next != NULL)`。如果此时链表为空（只有头节点），以下说法正确的是：
A. 循环条件 `p->next != NULL` 不成立，直接跳过，不会出错  
B. 循环会执行一次，然后段错误  
C. 会打印头节点中的无效数据  
D. 编译会报错  

你的答案: _____

---

**9.** 单向循环链表与普通单向链表相比，最显著的区别是：
A. 单向循环链表不需要头节点  
B. 单向循环链表的尾节点的 next 指向头节点，而不是 NULL  
C. 单向循环链表只能从尾部向头部遍历  
D. 单向循环链表的节点多了一个 prev 指针  

你的答案: _____

---

**10.** 用单向循环链表解决约瑟夫环问题时，使用头指针 h 进行遍历，以下哪种条件表示链表中只剩下一个节点？
A. `h == NULL`  
B. `h->next == NULL`  
C. `h->next == h`  
D. `h->data == -1`  

你的答案: _____

---

**11.** 在单向链表中，已知某个节点的指针 p，要删除 p 指向的节点（不是最后一个），但**不知道 p 的前驱节点**。以下说法正确的是：
A. 单向链表中这种情况无法删除，必须知道前驱  
B. 可以通过将 p->next 的数据复制到 p，然后删除 p->next 来变相删除  
C. 双向链表也存在同样的问题  
D. 可以直接 free(p)，不会影响链表  

你的答案: _____

---

**12.** 以下关于链式栈的说法，**错误**的是：
A. 链式栈的入栈和出栈操作都在栈顶进行  
B. 链式栈不需要判满  
C. 链式栈的入栈操作使用头插法  
D. 链式栈出栈时，需要将栈顶指针移动到下一个节点，然后释放原栈顶  

你的答案: _____

---

**13.** 入栈序列为 1, 2, 3, 4，以下哪个出栈序列是不可能出现的？
A. 1, 2, 3, 4  
B. 2, 4, 3, 1  
C. 4, 2, 3, 1  
D. 3, 4, 2, 1  

你的答案: _____

---

**14.** 以下程序（函数 get_mem 定义如下），关于它的说法正确的是：
```c
void get_mem(int *q)
{
    q = (int *)malloc(sizeof(int) * 10);
}

int main()
{
    int *p = NULL;
    get_mem(p);
    for (int i = 0; i < 10; i++)
        p[i] = i;
    free(p);
    return 0;
}
```
A. 程序正常运行，p 指向了堆区空间  
B. 程序出错，因为 get_mem 函数修改的是形参 q，不会影响实参 p  
C. 程序出错，因为 malloc 开辟的空间大小不够  
D. 程序出错，因为没有释放空间  

你的答案: _____

---

**15.** 循环队列中，假设数组容量为 N，front 指向队头，rear 指向队尾的下一个位置。最多可以存储多少个有效元素？
A. N  
B. N - 1  
C. N + 1  
D. 不确定  

你的答案: _____

---

**16.** 链式队列中，通常将头指针 front 和尾指针 rear 封装在一个结构体中。以下关于链式队列的说法正确的是：
A. front 指向头节点（不存数据），rear 指向最后一个有效节点  
B. 入队操作在 front 处进行，出队操作在 rear 处进行  
C. 链式队列判空的条件是 front == NULL  
D. 链式队列的入队操作使用头插法  

你的答案: _____

---

**17.** 循环队列初始状态 front=0, rear=0。执行 3 次入队、2 次出队后，front 和 rear 的值分别是（N=6）：
A. front=2, rear=3  
B. front=3, rear=0  
C. front=2, rear=0  
D. front=5, rear=0  

你的答案: _____

---

**18.** 双向链表中，已知一个非空非尾的节点指针 p，删除 p 指向的节点。以下哪段代码是正确的？
A.
```c
p->prior->next = p->next;
p->next->prior = p->prior;
free(p);
```
B.
```c
p->next->prior = p->prior;
p->prior->next = p->next;
free(p);
```
C.
```c
p->prior = p->next;
p->next = p->prior;
free(p);
```
D. A 和 B 都正确  

你的答案: _____

---

**19.** 关于双向链表和单向链表的比较，以下说法**错误**的是：
A. 双向链表的每个节点比单向链表多一个指针，占用更多内存  
B. 在已知要删除节点的指针时，双向链表可以 O(1) 删除，单向链表需要 O(n) 找到前驱  
C. 双向链表支持从尾向头遍历，单向链表不支持  
D. 双向链表的插入操作永远比单向链表快  

你的答案: _____

---

**20.** 双向循环链表中，头节点的 prior 指向：
A. NULL  
B. 头节点自身  
C. 尾节点  
D. 第一个有效节点  

你的答案: _____

---

**21.** 深度为 k（根为第 1 层）的二叉树，最多有多少个节点？
A. 2^k  
B. 2^k - 1  
C. 2^(k-1)  
D. 2^(k-1) - 1  

你的答案: _____

---

**22.** 在完全二叉树中，如果根节点编号为 1，那么编号为 i 的节点的左孩子编号为（如果存在的话）：
A. i + 1  
B. i * 2  
C. i * 2 + 1  
D. i / 2  

你的答案: _____

---

**23.** 已知一棵二叉树的前序遍历结果为 `A B D E C F`，中序遍历结果为 `D B E A F C`，其后序遍历结果为：
A. D E B F C A  
B. D E B F A C  
C. D E B C A F  
D. B D E F C A  

你的答案: _____

---

**24.** 二叉树的层次遍历（从上到下、从左到右）通常借助什么数据结构实现？
A. 栈  
B. 队列  
C. 数组  
D. 链表  

你的答案: _____

---

**25.** 若二叉树用链式存储，以下函数的功能是：
```c
int func(bitree_node_p root)
{
    if (root == NULL) return 0;
    int left = func(root->lchild);
    int right = func(root->rchild);
    return (left > right ? left : right) + 1;
}
```
A. 计算二叉树节点总数  
B. 计算二叉树中值为偶数的节点个数  
C. 计算二叉树深度  
D. 计算二叉树叶子节点个数  

你的答案: _____

---

# 第二部分：编程题（共5题，每题10分，共50分）

---

**1. 单向循环链表 -- 约瑟夫环（10分）**

已知单向循环链表节点定义：
```c
typedef struct node
{
    int data;
    struct node *next;
}node_t, *node_p;
```

请实现函数 `int josephus(int n, int k, int m)`，模拟约瑟夫环问题：
- n 个人围成一圈，编号从 1 到 n
- 从编号为 k 的人开始报数
- 数到 m 的人出列
- 下一个人从 1 开始重新报数
- 重复以上过程，直到剩下最后一个人
- 函数返回最后剩下的人的编号

要求：在函数内部创建一个包含 n 个节点的单向循环链表来模拟。

**提示**：创建单向循环链表的关键步骤是：创建 n 个节点连接成单向链表后，将尾节点的 next 指向头节点。

```c
int josephus(int n, int k, int m)
{
    // 请在此处实现
}
```

---

**2. 二叉树层次遍历（10分）**

二叉树节点定义：
```c
typedef int datatype;
typedef struct binary_node
{
    datatype data;
    struct binary_node *lchild;
    struct binary_node *rchild;
}bin_node_t, *bin_node_p;
```

队列相关操作函数（已实现，可直接使用）：
```c
// 循环队列，存储二叉树节点指针
typedef struct
{
    bin_node_p data[N];
    int front;
    int rear;
}queue_t, *queue_p;

queue_p create_queue();                 // 创建空队列
int enqueue(queue_p q, bin_node_p x);  // 入队，返回 0 成功 -1 失败
int dequeue(queue_p q, bin_node_p *x); // 出队，返回 0 成功 -1 失败
int is_empty_queue(queue_p q);         // 判空，空返回 1
```

要求：**不使用递归**，请实现函数 `void level_order(bin_node_p root)`，对二叉树进行**层次遍历**（从上到下、从左到右依次输出节点的 data 值）。

例如对以下二叉树：
```
          [1]
         /   \
       [2]   [3]
      /  \     \
    [4]  [5]   [6]
```
层次遍历输出：`1 2 3 4 5 6`

**提示**：根节点入队 -> 循环出队并访问 -> 左孩子非空入队 -> 右孩子非空入队。

```c
void level_order(bin_node_p root)
{
    // 请在此处实现
}
```

---

**3. 根据前序和中序还原二叉树（10分）**

二叉树节点定义：
```c
typedef int datatype;
typedef struct binary_node
{
    datatype data;
    struct binary_node *lchild;
    struct binary_node *rchild;
}bin_node_t, *bin_node_p;
```

前序遍历结果保存在数组 `pre[]` 中，中序遍历结果保存在数组 `in[]` 中，数组长度为 n。

请实现函数 `bin_node_p build_tree(int pre[], int in[], int n)`，根据前序和中序遍历结果还原一棵二叉树，并返回根节点指针。

**提示**：
- 前序的第一个元素是根节点
- 在中序中找到根节点，根节点左边的是左子树的中序，右边的是右子树的中序
- 根据左子树节点个数，在前序中划分出左子树和右子树的前序
- 递归构建左右子树

```c
bin_node_p build_tree(int pre[], int in[], int n)
{
    // 请在此处实现
}
```

---

**4. 链式队列实现（10分）**

链式队列的节点和队列结构体定义如下：
```c
typedef int datatype;
typedef struct node
{
    datatype data;
    struct node *next;
}link_node_t, *link_node_p;

typedef struct
{
    link_node_p front;  // 队头指针，指向头节点
    link_node_p rear;   // 队尾指针，指向最后一个有效节点
    int len;            // 队列长度
}linkqueue_t, *linkqueue_p;
```

请实现以下函数：
(1) `linkqueue_p create_queue()` -- 创建空链式队列（含头节点）
(2) `int enqueue(linkqueue_p q, datatype data)` -- 入队（尾插）
(3) `datatype dequeue(linkqueue_p q)` -- 出队（头删），返回出队数据
(4) `int is_empty(linkqueue_p q)` -- 判空

```c
// (1) 创建空链式队列
linkqueue_p create_queue()
{
    // 请在此处实现
}

// (2) 入队
int enqueue(linkqueue_p q, datatype data)
{
    // 请在此处实现
}

// (3) 出队
datatype dequeue(linkqueue_p q)
{
    // 请在此处实现
}

// (4) 判空
int is_empty(linkqueue_p q)
{
    // 请在此处实现
}
```

---

**5. 二叉树的度统计（10分）**

二叉树节点定义：
```c
typedef int datatype;
typedef struct binary_node
{
    datatype data;
    struct binary_node *lchild;
    struct binary_node *rchild;
}bin_node_t, *bin_node_p;
```

请实现函数 `void count_degree(bin_node_p root, int *n0, int *n1, int *n2)`，统计二叉树中度为 0、度为 1、度为 2 的节点个数。

- 度为 0：叶子节点（左右孩子都为空）
- 度为 1：只有一个孩子（左孩子为空右孩子不为空，或左孩子不为空右孩子为空）
- 度为 2：左右孩子都不为空

统计结果通过指针参数 `n0`、`n1`、`n2` 返回。

例如对以下二叉树：
```
          [1]
         /   \
       [2]   [3]
      /  \
    [4]  [5]
```
结果为：n0=2 (节点4、5、3)，n1=1 (节点2)，n2=2 (节点1、3)

```c
void count_degree(bin_node_p root, int *n0, int *n1, int *n2)
{
    // 请在此处实现
}
```

---

# 试卷结束

请在答题区作答。
选择题直接在选项上打勾或写字母，编程题写出完整的函数实现。